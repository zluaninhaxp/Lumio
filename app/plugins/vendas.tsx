import { useMemo, useState } from 'react';
import { View, Text, StyleSheet, TouchableOpacity, Modal, TextInput, Alert, ScrollView } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Ionicons } from '@expo/vector-icons';
import { useRouter } from 'expo-router';
import { Colors, Spacing, Radius, FontSize } from '../../src/constants/theme';
import { OrderItem, OrderStatus, Pedido, useAppStore } from '../../src/store';

type DraftItem = Omit<OrderItem, 'id'> & { id: string };
const money = (value: number) => `R$ ${value.toFixed(2).replace('.', ',')}`;
const todayLabel = () => new Date().toLocaleDateString('pt-BR', { day: '2-digit', month: '2-digit' });
const emptyItem = (): DraftItem => ({ id: `${Date.now()}-${Math.random()}`, name: '', quantity: 1, unitPrice: 0 });

export default function VendasScreen() {
  const router = useRouter();
  const { pedidos, clienteItems, estoqueItems, employeeItems, addPedido, updatePedido, completePedido, removePedido, setPluginActivation } = useAppStore();
  const [query, setQuery] = useState('');
  const [modalVisible, setModalVisible] = useState(false);
  const [editingId, setEditingId] = useState<string | null>(null);
  const [clientId, setClientId] = useState<string | undefined>();
  const [items, setItems] = useState<DraftItem[]>([emptyItem()]);
  const [status, setStatus] = useState<OrderStatus>('aberto');
  const [employeeId, setEmployeeId] = useState<string | undefined>();
  const [employeePickerVisible, setEmployeePickerVisible] = useState(false);

  const visibleOrders = useMemo(() => {
    const normalized = query.trim().toLowerCase();
    return pedidos.filter((order) => !normalized || order.id.toLowerCase().includes(normalized) || order.items.some((item) => item.name.toLowerCase().includes(normalized)) || clienteItems.find((client) => client.id === order.clientId)?.name.toLowerCase().includes(normalized));
  }, [pedidos, query, clienteItems]);

  const total = items.reduce((sum, item) => sum + (Number(item.quantity) || 0) * (Number(item.unitPrice) || 0), 0);
  const openAdd = () => { setEditingId(null); setClientId(undefined); setEmployeeId(undefined); setItems([emptyItem()]); setStatus('aberto'); setModalVisible(true); setEmployeePickerVisible(true); };
  const openEdit = (order: Pedido) => { setEditingId(order.id); setClientId(order.clientId); setEmployeeId(order.employeeId); setItems(order.items.map((item) => ({ ...item }))); setStatus(order.status); setModalVisible(true); assignEmployee(order); };
  const updateItem = (id: string, updates: Partial<DraftItem>) => setItems((current) => current.map((item) => item.id === id ? { ...item, ...updates } : item));
  const saveOrder = () => {
    const validItems = items.filter((item) => item.name.trim() && Number(item.quantity) > 0 && Number(item.unitPrice) >= 0).map((item) => ({ ...item, name: item.name.trim(), quantity: Number(item.quantity), unitPrice: Number(item.unitPrice) }));
    if (validItems.length === 0) return;
    const orderTotal = validItems.reduce((sum, item) => sum + item.quantity * item.unitPrice, 0);
    const current = editingId ? pedidos.find((order) => order.id === editingId) : undefined;
    const payload = { clientId, employeeId, items: validItems, total: orderTotal, status, date: current?.date ?? todayLabel(), createdAt: current?.createdAt ?? new Date().toISOString() };
    const ok = editingId ? updatePedido(editingId, payload) : (addPedido(payload), true);
    if (!ok) { Alert.alert('Pedido não atualizado', 'A quantidade disponível no estoque não é suficiente para concluir este pedido.'); return; }
    setModalVisible(false);
  };
  const conclude = (id: string) => {
    if (!completePedido(id)) {
      Alert.alert('Não foi possível concluir', 'Verifique se há quantidade suficiente no Estoque para os itens cadastrados.');
      return;
    }
    Alert.alert('Pedido concluído', 'Quer gerar uma entrega vinculada a este pedido?', [
      { text: 'Agora', onPress: () => router.push(`/plugins/entregas?orderId=${id}` as any) },
      { text: 'Depois', style: 'cancel' },
    ]);
  };
  const cancel = (id: string) => { if (!updatePedido(id, { status: 'cancelado' })) Alert.alert('Não foi possível cancelar o pedido.'); };
  const assignEmployee = (order: Pedido) => Alert.alert('Quem atendeu este pedido?', undefined, [
    { text: 'Não informado', onPress: () => updatePedido(order.id, { employeeId: undefined }) },
    ...employeeItems.map((employee) => ({ text: employee.name, onPress: () => updatePedido(order.id, { employeeId: employee.id }) })),
    { text: 'Cancelar', style: 'cancel' as const },
  ]);
  const clientName = (id?: string) => id ? clienteItems.find((client) => client.id === id)?.name : undefined;
  const statusLabel: Record<OrderStatus, string> = { aberto: 'Aberto', concluido: 'Concluído', cancelado: 'Cancelado' };

  return <SafeAreaView style={styles.safe}>
    <View style={styles.header}><TouchableOpacity onPress={() => router.back()} style={styles.iconBtn}><Ionicons name="chevron-back" size={24} color={Colors.primary} /></TouchableOpacity><Text style={styles.headerTitle}>Pedidos / Vendas</Text><TouchableOpacity onPress={() => Alert.alert('Desativar Pedidos', 'O módulo sai da aba Apps, mas os dados continuam guardados.', [{ text: 'Cancelar', style: 'cancel' }, { text: 'Desativar', style: 'destructive', onPress: () => { setPluginActivation('vendas', false); router.back(); } }])} style={styles.iconBtn}><Ionicons name="ellipsis-horizontal" size={22} color={Colors.primary} /></TouchableOpacity></View>
    <View style={styles.searchBox}><Ionicons name="search-outline" size={18} color={Colors.textMuted} /><TextInput value={query} onChangeText={setQuery} placeholder="Buscar pedido ou item" placeholderTextColor={Colors.textMuted} style={styles.searchInput} /></View>
    <ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}>
      {visibleOrders.length === 0 && <View style={styles.empty}><Ionicons name="receipt-outline" size={46} color={Colors.textMuted} /><Text style={styles.emptyText}>{query ? 'Nenhum pedido encontrado.' : 'Nenhum pedido cadastrado ainda.'}</Text></View>}
      {visibleOrders.map((order) => <View key={order.id} style={styles.card}><View style={styles.cardHeader}><View style={styles.orderIcon}><Ionicons name="receipt-outline" size={18} color={Colors.accent} /></View><View style={styles.cardMain}><Text style={styles.cardTitle}>Pedido {order.id.slice(-6)}</Text><Text style={styles.cardSubtitle}>{order.date}{clientName(order.clientId) ? ` · ${clientName(order.clientId)}` : ' · Venda avulsa'}</Text></View><View style={[styles.statusBadge, order.status === 'concluido' && styles.statusDone, order.status === 'cancelado' && styles.statusCanceled]}><Text style={styles.statusText}>{statusLabel[order.status]}</Text></View></View><Text style={styles.itemSummary}>{order.items.map((item) => `${item.quantity}x ${item.name}`).join(' · ')}</Text><View style={styles.cardFooter}><Text style={styles.total}>{money(order.total)}</Text><View style={styles.actions}>{order.status === 'aberto' && <TouchableOpacity onPress={() => conclude(order.id)}><Text style={styles.concludeText}>Concluir</Text></TouchableOpacity>}<TouchableOpacity onPress={() => openEdit(order)}><Text style={styles.actionText}>Editar</Text></TouchableOpacity>{order.status !== 'cancelado' && <TouchableOpacity onPress={() => cancel(order.id)}><Text style={styles.cancelText}>Cancelar</Text></TouchableOpacity>}</View></View></View>)}
    </ScrollView>
    <TouchableOpacity style={styles.fab} onPress={openAdd}><Ionicons name="add" size={28} color="#FFFFFF" /></TouchableOpacity>
    <Modal visible={modalVisible} transparent animationType="slide" onRequestClose={() => setModalVisible(false)}><View style={styles.modalOverlay}><View style={styles.modalCard}><ScrollView showsVerticalScrollIndicator={false}><Text style={styles.modalTitle}>{editingId ? 'Editar pedido' : 'Novo pedido'}</Text><Text style={styles.label}>Cliente (opcional)</Text><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.chipRow}><TouchableOpacity style={[styles.chip, !clientId && styles.chipActive]} onPress={() => setClientId(undefined)}><Text style={[styles.chipText, !clientId && styles.chipTextActive]}>Venda avulsa</Text></TouchableOpacity>{clienteItems.map((client) => <TouchableOpacity key={client.id} style={[styles.chip, clientId === client.id && styles.chipActive]} onPress={() => setClientId(client.id)}><Text style={[styles.chipText, clientId === client.id && styles.chipTextActive]}>{client.name}</Text></TouchableOpacity>)}</ScrollView><Text style={styles.label}>Itens vendidos</Text>{items.map((item, index) => <View key={item.id} style={styles.itemForm}><TextInput style={styles.itemNameInput} value={item.name} onChangeText={(value) => updateItem(item.id, { name: value })} placeholder={`Item ${index + 1}`} placeholderTextColor={Colors.textMuted} /><View style={styles.numberRow}><TextInput style={styles.numberInput} value={String(item.quantity)} onChangeText={(value) => updateItem(item.id, { quantity: Number(value.replace(',', '.')) || 0 })} placeholder="Qtd." placeholderTextColor={Colors.textMuted} keyboardType="decimal-pad" /><TextInput style={styles.numberInput} value={String(item.unitPrice || '')} onChangeText={(value) => updateItem(item.id, { unitPrice: Number(value.replace(',', '.')) || 0 })} placeholder="Preço unit." placeholderTextColor={Colors.textMuted} keyboardType="decimal-pad" /></View><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.chipRow}><TouchableOpacity style={[styles.stockChip, !item.stockItemId && styles.stockChipActive]} onPress={() => updateItem(item.id, { stockItemId: undefined })}><Text style={[styles.chipText, !item.stockItemId && styles.chipTextActive]}>Sem estoque</Text></TouchableOpacity>{estoqueItems.map((stock) => <TouchableOpacity key={stock.id} style={[styles.stockChip, item.stockItemId === stock.id && styles.stockChipActive]} onPress={() => updateItem(item.id, { stockItemId: stock.id, name: item.name || stock.name })}><Text style={[styles.chipText, item.stockItemId === stock.id && styles.chipTextActive]}>{stock.name}</Text></TouchableOpacity>)}</ScrollView></View>)}<TouchableOpacity style={styles.addItemButton} onPress={() => setItems((current) => [...current, emptyItem()])}><Ionicons name="add" size={17} color={Colors.accent} /><Text style={styles.actionText}>Adicionar item</Text></TouchableOpacity><Text style={styles.totalPreview}>Total: {money(total)}</Text>{editingId && <><Text style={styles.label}>Status</Text><View style={styles.chipRow}>{(['aberto', 'concluido', 'cancelado'] as OrderStatus[]).map((option) => <TouchableOpacity key={option} style={[styles.chip, status === option && styles.chipActive]} onPress={() => setStatus(option)}><Text style={[styles.chipText, status === option && styles.chipTextActive]}>{statusLabel[option]}</Text></TouchableOpacity>)}</View></>}</ScrollView><View style={styles.modalActions}><TouchableOpacity style={styles.modalCancel} onPress={() => setModalVisible(false)}><Text style={styles.modalCancelText}>Cancelar</Text></TouchableOpacity><TouchableOpacity style={styles.modalConfirm} onPress={saveOrder}><Text style={styles.modalConfirmText}>Salvar</Text></TouchableOpacity></View></View></View></Modal>
     <Modal visible={employeePickerVisible} transparent animationType="fade" onRequestClose={() => setEmployeePickerVisible(false)}><View style={styles.modalOverlay}><View style={styles.employeePickerCard}><Text style={styles.modalTitle}>Quem atendeu esta venda?</Text><Text style={styles.pickerHint}>Você pode alterar essa informação depois.</Text><TouchableOpacity style={styles.employeeOption} onPress={() => { setEmployeeId(undefined); setEmployeePickerVisible(false); }}><Text style={styles.employeeOptionText}>Não informado</Text></TouchableOpacity>{employeeItems.map((employee) => <TouchableOpacity key={employee.id} style={styles.employeeOption} onPress={() => { setEmployeeId(employee.id); setEmployeePickerVisible(false); }}><Text style={styles.employeeOptionText}>{employee.name}</Text><Text style={styles.employeeRoleText}>{employee.role}</Text></TouchableOpacity>)}</View></View></Modal>
   </SafeAreaView>;
}

const styles = StyleSheet.create({ employeePickerCard: { backgroundColor: Colors.bgCard, borderRadius: Radius.xl, marginHorizontal: Spacing.xl, padding: Spacing.xxl, gap: Spacing.sm }, pickerHint: { color: Colors.textSecondary, fontSize: FontSize.sm, marginBottom: Spacing.sm }, employeeOption: { paddingVertical: Spacing.md, borderTopWidth: 1, borderTopColor: Colors.border }, employeeOptionText: { color: Colors.primary, fontFamily: 'PlusJakartaSans_600SemiBold', fontSize: FontSize.md }, employeeRoleText: { color: Colors.textSecondary, fontSize: FontSize.sm, marginTop: 2 },
  safe: { flex: 1, backgroundColor: Colors.bg }, header: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: Spacing.lg, paddingVertical: Spacing.md }, iconBtn: { padding: Spacing.xs }, headerTitle: { fontFamily: 'PlusJakartaSans_700Bold', fontSize: FontSize.lg, color: Colors.primary }, searchBox: { flexDirection: 'row', alignItems: 'center', gap: Spacing.sm, marginHorizontal: Spacing.xl, marginBottom: Spacing.md, paddingHorizontal: Spacing.lg, backgroundColor: Colors.bgCard, borderRadius: Radius.full, borderWidth: 1, borderColor: Colors.border }, searchInput: { flex: 1, height: 44, color: Colors.primary }, content: { paddingHorizontal: Spacing.xl, paddingBottom: 100 }, empty: { alignItems: 'center', paddingTop: 80, gap: Spacing.md }, emptyText: { fontFamily: 'PlusJakartaSans_600SemiBold', color: Colors.textSecondary }, card: { backgroundColor: Colors.bgCard, borderRadius: Radius.lg, padding: Spacing.lg, marginBottom: Spacing.sm }, cardHeader: { flexDirection: 'row', alignItems: 'center', gap: Spacing.md }, orderIcon: { width: 38, height: 38, borderRadius: 19, backgroundColor: Colors.accentLight, alignItems: 'center', justifyContent: 'center' }, cardMain: { flex: 1 }, cardTitle: { fontFamily: 'PlusJakartaSans_600SemiBold', color: Colors.primary, fontSize: FontSize.md }, cardSubtitle: { color: Colors.textSecondary, fontSize: FontSize.sm, marginTop: 2 }, statusBadge: { backgroundColor: '#FFF4D6', borderRadius: Radius.full, paddingHorizontal: Spacing.sm, paddingVertical: 5 }, statusDone: { backgroundColor: Colors.accentLight }, statusCanceled: { backgroundColor: Colors.dangerLight }, statusText: { color: Colors.primary, fontSize: FontSize.xs, fontFamily: 'PlusJakartaSans_600SemiBold' }, itemSummary: { color: Colors.textSecondary, fontSize: FontSize.sm, marginTop: Spacing.md }, cardFooter: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', marginTop: Spacing.md }, total: { color: Colors.accent, fontFamily: 'PlusJakartaSans_700Bold', fontSize: FontSize.md }, actions: { flexDirection: 'row', gap: Spacing.md }, concludeText: { color: Colors.accent, fontFamily: 'PlusJakartaSans_600SemiBold', fontSize: FontSize.sm }, actionText: { color: Colors.accent, fontFamily: 'PlusJakartaSans_600SemiBold', fontSize: FontSize.sm }, cancelText: { color: Colors.danger, fontFamily: 'PlusJakartaSans_600SemiBold', fontSize: FontSize.sm }, fab: { position: 'absolute', bottom: 24, right: 24, width: 56, height: 56, borderRadius: 28, backgroundColor: Colors.accent, alignItems: 'center', justifyContent: 'center', elevation: 6 }, modalOverlay: { flex: 1, backgroundColor: 'rgba(0,0,0,0.4)', justifyContent: 'flex-end' }, modalCard: { backgroundColor: Colors.bgCard, borderTopLeftRadius: Radius.xl, borderTopRightRadius: Radius.xl, padding: Spacing.xxl, paddingBottom: 40, gap: Spacing.md, maxHeight: '90%' }, modalTitle: { fontFamily: 'PlusJakartaSans_700Bold', color: Colors.primary, fontSize: FontSize.xl, marginBottom: Spacing.md }, label: { fontFamily: 'PlusJakartaSans_600SemiBold', color: Colors.textSecondary, fontSize: FontSize.sm, marginTop: Spacing.sm }, chipRow: { flexDirection: 'row', gap: Spacing.sm, paddingVertical: Spacing.sm }, chip: { borderWidth: 1, borderColor: Colors.border, borderRadius: Radius.full, paddingHorizontal: Spacing.md, paddingVertical: Spacing.sm }, chipActive: { backgroundColor: Colors.primary, borderColor: Colors.primary }, chipText: { color: Colors.textSecondary, fontSize: FontSize.xs }, chipTextActive: { color: '#FFFFFF' }, itemForm: { backgroundColor: Colors.bg, borderRadius: Radius.md, padding: Spacing.md, marginTop: Spacing.sm, gap: Spacing.sm }, itemNameInput: { backgroundColor: Colors.bgCard, borderRadius: Radius.sm, padding: Spacing.md, color: Colors.primary }, numberRow: { flexDirection: 'row', gap: Spacing.sm }, numberInput: { flex: 1, backgroundColor: Colors.bgCard, borderRadius: Radius.sm, padding: Spacing.md, color: Colors.primary }, stockChip: { borderWidth: 1, borderColor: Colors.border, borderRadius: Radius.full, paddingHorizontal: Spacing.sm, paddingVertical: 6 }, stockChipActive: { backgroundColor: Colors.accent, borderColor: Colors.accent }, addItemButton: { flexDirection: 'row', alignItems: 'center', gap: Spacing.xs, paddingVertical: Spacing.md }, totalPreview: { textAlign: 'right', color: Colors.primary, fontFamily: 'PlusJakartaSans_700Bold', fontSize: FontSize.lg, marginVertical: Spacing.md }, modalActions: { flexDirection: 'row', gap: Spacing.md, marginTop: Spacing.md }, modalCancel: { flex: 1, padding: Spacing.lg, borderRadius: Radius.md, borderWidth: 1, borderColor: Colors.border, alignItems: 'center' }, modalCancelText: { color: Colors.textSecondary, fontFamily: 'PlusJakartaSans_600SemiBold' }, modalConfirm: { flex: 1, padding: Spacing.lg, borderRadius: Radius.md, backgroundColor: Colors.accent, alignItems: 'center' }, modalConfirmText: { color: '#FFFFFF', fontFamily: 'PlusJakartaSans_600SemiBold' },
});
