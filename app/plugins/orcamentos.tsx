import { useEffect, useMemo, useState } from 'react';
import { View, Text, StyleSheet, TouchableOpacity, Modal, TextInput, Alert, ScrollView } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Ionicons } from '@expo/vector-icons';
import { useRouter } from 'expo-router';
import { Colors, Spacing, Radius, FontSize } from '../../src/constants/theme';
import { OrderItem, Orcamento, QuoteStatus, useAppStore } from '../../src/store';

type DraftItem = OrderItem;
const money = (value: number) => `R$ ${value.toFixed(2).replace('.', ',')}`;
const emptyItem = (): DraftItem => ({ id: `${Date.now()}-${Math.random()}`, name: '', quantity: 1, unitPrice: 0 });
const defaultValidity = () => { const date = new Date(); date.setDate(date.getDate() + 7); return date.toISOString().split('T')[0]; };

export default function OrcamentosScreen() {
  const router = useRouter();
  const { orcamentos, clienteItems, addOrcamento, updateOrcamento, approveOrcamento, refreshOrcamentos, setPluginActivation } = useAppStore();
  const [query, setQuery] = useState('');
  const [modalVisible, setModalVisible] = useState(false);
  const [editingId, setEditingId] = useState<string | null>(null);
  const [clientId, setClientId] = useState<string | undefined>();
  const [validUntil, setValidUntil] = useState(defaultValidity());
  const [items, setItems] = useState<DraftItem[]>([emptyItem()]);
  useEffect(() => { refreshOrcamentos(); }, [refreshOrcamentos]);
  const visible = useMemo(() => { const q = query.trim().toLowerCase(); return orcamentos.filter((quote) => !q || quote.id.toLowerCase().includes(q) || quote.items.some((item) => item.name.toLowerCase().includes(q)) || clienteItems.find((client) => client.id === quote.clientId)?.name.toLowerCase().includes(q)); }, [orcamentos, query, clienteItems]);
  const total = items.reduce((sum, item) => sum + item.quantity * item.unitPrice, 0);
  const statusLabel: Record<QuoteStatus, string> = { pendente: 'Pendente', aprovado: 'Aprovado', recusado: 'Recusado', expirado: 'Expirado' };
  const openAdd = () => { setEditingId(null); setClientId(undefined); setValidUntil(defaultValidity()); setItems([emptyItem()]); setModalVisible(true); };
  const openEdit = (quote: Orcamento) => { setEditingId(quote.id); setClientId(quote.clientId); setValidUntil(quote.validUntil); setItems(quote.items.map((item) => ({ ...item }))); setModalVisible(true); };
  const updateItem = (id: string, updates: Partial<DraftItem>) => setItems((current) => current.map((item) => item.id === id ? { ...item, ...updates } : item));
  const save = () => { const validItems = items.filter((item) => item.name.trim() && item.quantity > 0).map((item) => ({ ...item, name: item.name.trim() })); if (!validItems.length || !validUntil.trim()) return; const current = editingId ? orcamentos.find((quote) => quote.id === editingId) : undefined; if (current && current.status !== 'pendente') { Alert.alert('Orçamento encerrado', 'Orçamentos aprovados, recusados ou expirados não podem ser editados.'); return; } const payload = { clientId, items: validItems, total: validItems.reduce((sum, item) => sum + item.quantity * item.unitPrice, 0), validUntil: validUntil.trim(), status: current?.status ?? 'pendente' as QuoteStatus, createdAt: current?.createdAt ?? new Date().toISOString() }; if (editingId) updateOrcamento(editingId, payload); else addOrcamento(payload); setModalVisible(false); };
  const approve = (id: string) => { const orderId = approveOrcamento(id); if (!orderId) Alert.alert('Orçamento não aprovado', 'Ele pode já ter sido convertido, estar expirado ou não estar pendente.'); else Alert.alert('Orçamento aprovado', `Pedido ${orderId.slice(-6)} criado. Conclua o pedido em Pedidos / Vendas para gerar a receita.`); };
  const clientName = (id?: string) => id ? clienteItems.find((client) => client.id === id)?.name : undefined;
  return <SafeAreaView style={styles.safe}><View style={styles.header}><TouchableOpacity onPress={() => router.back()} style={styles.iconBtn}><Ionicons name="chevron-back" size={24} color={Colors.primary} /></TouchableOpacity><Text style={styles.headerTitle}>Orçamentos</Text><TouchableOpacity onPress={() => Alert.alert('Desativar Orçamentos', 'O módulo sai da aba Apps, mas os dados continuam guardados.', [{ text: 'Cancelar', style: 'cancel' }, { text: 'Desativar', style: 'destructive', onPress: () => { setPluginActivation('orcamentos', false); router.back(); } }])} style={styles.iconBtn}><Ionicons name="ellipsis-horizontal" size={22} color={Colors.primary} /></TouchableOpacity></View><View style={styles.searchBox}><Ionicons name="search-outline" size={18} color={Colors.textMuted} /><TextInput value={query} onChangeText={setQuery} placeholder="Buscar orçamento" placeholderTextColor={Colors.textMuted} style={styles.searchInput} /></View><ScrollView contentContainerStyle={styles.content}>{visible.length === 0 && <View style={styles.empty}><Ionicons name="document-text-outline" size={46} color={Colors.textMuted} /><Text style={styles.emptyText}>{query ? 'Nenhum orçamento encontrado.' : 'Nenhum orçamento cadastrado ainda.'}</Text></View>}{visible.map((quote) => <View key={quote.id} style={styles.card}><View style={styles.cardHeader}><View style={styles.iconCircle}><Ionicons name="document-text-outline" size={18} color={Colors.accent} /></View><View style={styles.cardMain}><Text style={styles.cardTitle}>Orçamento {quote.id.slice(-6)}</Text><Text style={styles.cardSubtitle}>{clientName(quote.clientId) || 'Cliente não vinculado'} · válido até {quote.validUntil}</Text></View><View style={[styles.status, quote.status === 'aprovado' && styles.statusDone, (quote.status === 'recusado' || quote.status === 'expirado') && styles.statusBad]}><Text style={styles.statusText}>{statusLabel[quote.status]}</Text></View></View><Text style={styles.itemsText}>{quote.items.map((item) => `${item.quantity}x ${item.name}`).join(' · ')}</Text><View style={styles.footer}><Text style={styles.total}>{money(quote.total)}</Text><View style={styles.actions}>{quote.status === 'pendente' && <TouchableOpacity onPress={() => approve(quote.id)}><Text style={styles.approveText}>Aprovar</Text></TouchableOpacity>}{quote.status === 'pendente' && <TouchableOpacity onPress={() => updateOrcamento(quote.id, { status: 'recusado' })}><Text style={styles.rejectText}>Recusar</Text></TouchableOpacity>}<TouchableOpacity onPress={() => openEdit(quote)}><Text style={styles.actionText}>Editar</Text></TouchableOpacity></View></View>{quote.orderId && <Text style={styles.linked}>Convertido no pedido {quote.orderId.slice(-6)}</Text>}</View>)}</ScrollView><TouchableOpacity style={styles.fab} onPress={openAdd}><Ionicons name="add" size={28} color="#FFFFFF" /></TouchableOpacity><Modal visible={modalVisible} transparent animationType="slide" onRequestClose={() => setModalVisible(false)}><View style={styles.modalOverlay}><View style={styles.modalCard}><ScrollView><Text style={styles.modalTitle}>{editingId ? 'Editar orçamento' : 'Novo orçamento'}</Text><Text style={styles.label}>Cliente (opcional)</Text><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.chips}><TouchableOpacity style={[styles.chip, !clientId && styles.chipActive]} onPress={() => setClientId(undefined)}><Text style={[styles.chipText, !clientId && styles.chipTextActive]}>Sem cliente</Text></TouchableOpacity>{clienteItems.map((client) => <TouchableOpacity key={client.id} style={[styles.chip, clientId === client.id && styles.chipActive]} onPress={() => setClientId(client.id)}><Text style={[styles.chipText, clientId === client.id && styles.chipTextActive]}>{client.name}</Text></TouchableOpacity>)}</ScrollView><Text style={styles.label}>Validade (AAAA-MM-DD)</Text><TextInput style={styles.input} value={validUntil} onChangeText={setValidUntil} placeholder="AAAA-MM-DD" placeholderTextColor={Colors.textMuted} /><Text style={styles.label}>Itens propostos</Text>{items.map((item, index) => <View key={item.id} style={styles.itemForm}><TextInput style={styles.input} value={item.name} onChangeText={(value) => updateItem(item.id, { name: value })} placeholder={`Item ${index + 1}`} placeholderTextColor={Colors.textMuted} /><View style={styles.numberRow}><TextInput style={styles.numberInput} value={String(item.quantity)} onChangeText={(value) => updateItem(item.id, { quantity: Number(value) || 0 })} placeholder="Qtd." placeholderTextColor={Colors.textMuted} keyboardType="decimal-pad" /><TextInput style={styles.numberInput} value={String(item.unitPrice || '')} onChangeText={(value) => updateItem(item.id, { unitPrice: Number(value.replace(',', '.')) || 0 })} placeholder="Valor unitário" placeholderTextColor={Colors.textMuted} keyboardType="decimal-pad" /></View></View>)}<TouchableOpacity style={styles.addItem} onPress={() => setItems((current) => [...current, emptyItem()])}><Text style={styles.actionText}>+ Adicionar item</Text></TouchableOpacity><Text style={styles.totalPreview}>Total: {money(total)}</Text></ScrollView><View style={styles.modalActions}><TouchableOpacity style={styles.modalCancel} onPress={() => setModalVisible(false)}><Text style={styles.cancelLabel}>Cancelar</Text></TouchableOpacity><TouchableOpacity style={styles.modalConfirm} onPress={save}><Text style={styles.confirmLabel}>Salvar</Text></TouchableOpacity></View></View></View></Modal></SafeAreaView>;
}

const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: Colors.bg }, header: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: Spacing.lg, paddingVertical: Spacing.md }, iconBtn: { padding: Spacing.xs }, headerTitle: { fontFamily: 'PlusJakartaSans_700Bold', color: Colors.primary, fontSize: FontSize.lg }, searchBox: { flexDirection: 'row', alignItems: 'center', gap: Spacing.sm, marginHorizontal: Spacing.xl, marginBottom: Spacing.md, paddingHorizontal: Spacing.lg, backgroundColor: Colors.bgCard, borderRadius: Radius.full, borderWidth: 1, borderColor: Colors.border }, searchInput: { flex: 1, height: 44, color: Colors.primary }, content: { paddingHorizontal: Spacing.xl, paddingBottom: 100 }, empty: { alignItems: 'center', paddingTop: 80, gap: Spacing.md }, emptyText: { color: Colors.textSecondary, fontFamily: 'PlusJakartaSans_600SemiBold' }, card: { backgroundColor: Colors.bgCard, borderRadius: Radius.lg, padding: Spacing.lg, marginBottom: Spacing.sm }, cardHeader: { flexDirection: 'row', alignItems: 'center', gap: Spacing.md }, iconCircle: { width: 38, height: 38, borderRadius: 19, backgroundColor: Colors.accentLight, alignItems: 'center', justifyContent: 'center' }, cardMain: { flex: 1 }, cardTitle: { color: Colors.primary, fontFamily: 'PlusJakartaSans_600SemiBold', fontSize: FontSize.md }, cardSubtitle: { color: Colors.textSecondary, fontSize: FontSize.sm, marginTop: 2 }, status: { backgroundColor: '#FFF4D6', borderRadius: Radius.full, paddingHorizontal: Spacing.sm, paddingVertical: 5 }, statusDone: { backgroundColor: Colors.accentLight }, statusBad: { backgroundColor: Colors.dangerLight }, statusText: { color: Colors.primary, fontSize: FontSize.xs, fontFamily: 'PlusJakartaSans_600SemiBold' }, itemsText: { color: Colors.textSecondary, marginTop: Spacing.md }, footer: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginTop: Spacing.md }, total: { color: Colors.accent, fontFamily: 'PlusJakartaSans_700Bold', fontSize: FontSize.md }, actions: { flexDirection: 'row', gap: Spacing.md }, approveText: { color: Colors.accent, fontFamily: 'PlusJakartaSans_600SemiBold' }, rejectText: { color: Colors.danger, fontFamily: 'PlusJakartaSans_600SemiBold' }, actionText: { color: Colors.accent, fontFamily: 'PlusJakartaSans_600SemiBold' }, linked: { color: Colors.textMuted, fontSize: FontSize.xs, marginTop: Spacing.sm }, fab: { position: 'absolute', bottom: 24, right: 24, width: 56, height: 56, borderRadius: 28, backgroundColor: Colors.accent, alignItems: 'center', justifyContent: 'center', elevation: 6 }, modalOverlay: { flex: 1, backgroundColor: 'rgba(0,0,0,0.4)', justifyContent: 'flex-end' }, modalCard: { maxHeight: '90%', backgroundColor: Colors.bgCard, borderTopLeftRadius: Radius.xl, borderTopRightRadius: Radius.xl, padding: Spacing.xxl, paddingBottom: 40 }, modalTitle: { color: Colors.primary, fontFamily: 'PlusJakartaSans_700Bold', fontSize: FontSize.xl, marginBottom: Spacing.md }, label: { color: Colors.textSecondary, fontFamily: 'PlusJakartaSans_600SemiBold', fontSize: FontSize.sm, marginTop: Spacing.sm }, chips: { flexDirection: 'row', gap: Spacing.sm, paddingVertical: Spacing.sm }, chip: { borderWidth: 1, borderColor: Colors.border, borderRadius: Radius.full, paddingHorizontal: Spacing.md, paddingVertical: Spacing.sm }, chipActive: { backgroundColor: Colors.primary, borderColor: Colors.primary }, chipText: { color: Colors.textSecondary, fontSize: FontSize.xs }, chipTextActive: { color: '#FFFFFF' }, input: { backgroundColor: Colors.bg, borderRadius: Radius.md, padding: Spacing.lg, color: Colors.primary, marginTop: Spacing.xs }, itemForm: { backgroundColor: Colors.bg, borderRadius: Radius.md, padding: Spacing.md, marginTop: Spacing.sm, gap: Spacing.sm }, numberRow: { flexDirection: 'row', gap: Spacing.sm }, numberInput: { flex: 1, backgroundColor: Colors.bgCard, borderRadius: Radius.sm, padding: Spacing.md, color: Colors.primary }, addItem: { paddingVertical: Spacing.md }, totalPreview: { textAlign: 'right', color: Colors.primary, fontFamily: 'PlusJakartaSans_700Bold', fontSize: FontSize.lg, marginVertical: Spacing.md }, modalActions: { flexDirection: 'row', gap: Spacing.md, marginTop: Spacing.md }, modalCancel: { flex: 1, borderWidth: 1, borderColor: Colors.border, borderRadius: Radius.md, padding: Spacing.lg, alignItems: 'center' }, cancelLabel: { color: Colors.textSecondary, fontFamily: 'PlusJakartaSans_600SemiBold' }, modalConfirm: { flex: 1, backgroundColor: Colors.accent, borderRadius: Radius.md, padding: Spacing.lg, alignItems: 'center' }, confirmLabel: { color: '#FFFFFF', fontFamily: 'PlusJakartaSans_600SemiBold' }, });
