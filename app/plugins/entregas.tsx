import { useMemo, useState } from 'react';
import { Alert, Modal, ScrollView, StyleSheet, Text, TextInput, TouchableOpacity, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Ionicons } from '@expo/vector-icons';
import { useLocalSearchParams, useRouter } from 'expo-router';
import { Colors, FontSize, Radius, Spacing } from '../../src/constants/theme';
import { DeliveryStatus, Entrega, useAppStore } from '../../src/store';

const statusLabels: Record<DeliveryStatus, string> = { 'a caminho': 'A caminho', entregue: 'Entregue', cancelada: 'Cancelada' };
const nextDate = () => { const d = new Date(); d.setDate(d.getDate() + 1); return d.toISOString().split('T')[0]; };

export default function EntregasScreen() {
  const router = useRouter();
  const { orderId } = useLocalSearchParams<{ orderId?: string }>();
  const { entregas, pedidos, employeeItems, addEntrega, updateEntrega, removeEntrega, setPluginActivation } = useAppStore();
  const [modalVisible, setModalVisible] = useState(!!orderId);
  const [editingId, setEditingId] = useState<string | null>(null);
  const [selectedOrderId, setSelectedOrderId] = useState(orderId ?? '');
  const [employeeId, setEmployeeId] = useState<string | undefined>();
  const [address, setAddress] = useState('');
  const [estimatedDate, setEstimatedDate] = useState(nextDate());
  const [freightValue, setFreightValue] = useState('');
  const [createExpense, setCreateExpense] = useState(false);

  const activeDeliveries = useMemo(() => entregas.filter((item) => item.status !== 'cancelada'), [entregas]);
  const openAdd = () => { setEditingId(null); setSelectedOrderId(''); setEmployeeId(undefined); setAddress(''); setEstimatedDate(nextDate()); setFreightValue(''); setCreateExpense(false); setModalVisible(true); };
  const openEdit = (delivery: Entrega) => { setEditingId(delivery.id); setSelectedOrderId(delivery.orderId); setEmployeeId(delivery.employeeId); setAddress(delivery.address); setEstimatedDate(delivery.estimatedDate); setFreightValue(delivery.freightValue ? String(delivery.freightValue) : ''); setCreateExpense(false); setModalVisible(true); };
  const save = () => {
    if (!selectedOrderId || !address.trim() || !/^\d{4}-\d{2}-\d{2}$/.test(estimatedDate)) return;
    const value = Number(freightValue.replace(',', '.')) || 0;
    if (editingId) updateEntrega(editingId, { employeeId, address: address.trim(), estimatedDate, freightValue: value || undefined });
    else if (!addEntrega({ orderId: selectedOrderId, employeeId, address: address.trim(), estimatedDate, status: 'a caminho', freightValue: value || undefined, createdAt: new Date().toISOString() }, createExpense)) { Alert.alert('Entrega não criada', 'Este pedido já possui uma entrega ativa.'); return; }
    setModalVisible(false);
  };
  const orderLabel = (id: string) => `Pedido ${id.slice(-6)}${pedidos.find((item) => item.id === id)?.clientId ? '' : ' · avulso'}`;

  return <SafeAreaView style={styles.safe}>
    <View style={styles.header}><TouchableOpacity onPress={() => router.back()} style={styles.iconBtn}><Ionicons name="chevron-back" size={24} color={Colors.primary} /></TouchableOpacity><Text style={styles.title}>Entregas</Text><TouchableOpacity onPress={() => Alert.alert('Desativar Entregas', 'Os dados continuam guardados.', [{ text: 'Cancelar', style: 'cancel' }, { text: 'Desativar', style: 'destructive', onPress: () => { setPluginActivation('entregas', false); router.back(); } }])} style={styles.iconBtn}><Ionicons name="ellipsis-horizontal" size={22} color={Colors.primary} /></TouchableOpacity></View>
    <ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}>
      {activeDeliveries.length === 0 && <View style={styles.empty}><Ionicons name="bicycle-outline" size={46} color={Colors.textMuted} /><Text style={styles.emptyText}>Nenhuma entrega ativa.</Text><Text style={styles.hint}>Conclua um pedido e gere a entrega pelo módulo Pedidos.</Text></View>}
      {activeDeliveries.map((delivery) => <View key={delivery.id} style={styles.card}><View style={styles.cardTop}><View style={styles.deliveryIcon}><Ionicons name="bicycle-outline" size={20} color={Colors.accent} /></View><View style={styles.cardMain}><Text style={styles.cardTitle}>{orderLabel(delivery.orderId)}</Text><Text style={styles.cardSubtitle}>{delivery.address}</Text></View><View style={[styles.badge, delivery.status === 'entregue' && styles.doneBadge]}><Text style={styles.badgeText}>{statusLabels[delivery.status]}</Text></View></View><Text style={styles.deadline}>Prazo: {new Date(`${delivery.estimatedDate}T00:00:00`).toLocaleDateString('pt-BR')}</Text><Text style={styles.meta}>{delivery.employeeId ? `Responsável: ${employeeItems.find((item) => item.id === delivery.employeeId)?.name ?? 'não encontrado'}` : 'Sem responsável atribuído'}</Text><View style={styles.actions}>{delivery.status === 'a caminho' && <TouchableOpacity onPress={() => updateEntrega(delivery.id, { status: 'entregue' })}><Text style={styles.conclude}>Marcar entregue</Text></TouchableOpacity>}<TouchableOpacity onPress={() => openEdit(delivery)}><Text style={styles.action}>Editar</Text></TouchableOpacity><TouchableOpacity onPress={() => updateEntrega(delivery.id, { status: 'cancelada' })}><Text style={styles.cancel}>Cancelar</Text></TouchableOpacity></View></View>)}
    </ScrollView>
    <TouchableOpacity style={styles.fab} onPress={openAdd}><Ionicons name="add" size={28} color="#FFF" /></TouchableOpacity>
    <Modal visible={modalVisible} transparent animationType="slide" onRequestClose={() => setModalVisible(false)}><View style={styles.overlay}><View style={styles.modal}><ScrollView showsVerticalScrollIndicator={false}><Text style={styles.modalTitle}>{editingId ? 'Editar entrega' : 'Nova entrega'}</Text><Text style={styles.label}>Pedido</Text><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.chips}>{pedidos.filter((order) => order.status === 'concluido' && (!entregas.some((item) => item.orderId === order.id && item.status !== 'cancelada') || order.id === selectedOrderId)).map((order) => <TouchableOpacity key={order.id} style={[styles.chip, selectedOrderId === order.id && styles.chipActive]} onPress={() => setSelectedOrderId(order.id)}><Text style={[styles.chipText, selectedOrderId === order.id && styles.chipTextActive]}>{orderLabel(order.id)}</Text></TouchableOpacity>)}</ScrollView><Text style={styles.label}>Endereço de entrega</Text><TextInput style={styles.input} value={address} onChangeText={setAddress} placeholder="Rua, número, complemento" placeholderTextColor={Colors.textMuted} /><Text style={styles.label}>Prazo estimado (AAAA-MM-DD)</Text><TextInput style={styles.input} value={estimatedDate} onChangeText={setEstimatedDate} placeholder="2026-08-12" placeholderTextColor={Colors.textMuted} /><Text style={styles.label}>Responsável (opcional)</Text><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.chips}><TouchableOpacity style={[styles.chip, !employeeId && styles.chipActive]} onPress={() => setEmployeeId(undefined)}><Text style={[styles.chipText, !employeeId && styles.chipTextActive]}>Não informado</Text></TouchableOpacity>{employeeItems.map((employee) => <TouchableOpacity key={employee.id} style={[styles.chip, employeeId === employee.id && styles.chipActive]} onPress={() => setEmployeeId(employee.id)}><Text style={[styles.chipText, employeeId === employee.id && styles.chipTextActive]}>{employee.name}</Text></TouchableOpacity>)}</ScrollView><Text style={styles.label}>Frete simples (opcional)</Text><TextInput style={styles.input} value={freightValue} onChangeText={setFreightValue} keyboardType="decimal-pad" placeholder="Valor" placeholderTextColor={Colors.textMuted} />{!editingId && Number(freightValue.replace(',', '.')) > 0 && <TouchableOpacity style={styles.expenseOption} onPress={() => setCreateExpense((value) => !value)}><Ionicons name={createExpense ? 'checkbox' : 'square-outline'} size={21} color={Colors.accent} /><Text style={styles.expenseText}>Lançar como despesa no Financeiro</Text></TouchableOpacity>}</ScrollView><View style={styles.modalActions}><TouchableOpacity onPress={() => setModalVisible(false)}><Text style={styles.action}>Cancelar</Text></TouchableOpacity><TouchableOpacity style={styles.saveButton} onPress={save}><Text style={styles.saveText}>Salvar entrega</Text></TouchableOpacity></View></View></View></Modal>
  </SafeAreaView>;
}

const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: Colors.bg }, header: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: Spacing.lg, paddingVertical: Spacing.md }, iconBtn: { padding: Spacing.xs }, title: { fontFamily: 'PlusJakartaSans_700Bold', color: Colors.primary, fontSize: FontSize.lg }, content: { paddingHorizontal: Spacing.xl, paddingBottom: 100 }, empty: { alignItems: 'center', paddingTop: 90, gap: Spacing.sm }, emptyText: { color: Colors.textSecondary, fontFamily: 'PlusJakartaSans_600SemiBold' }, hint: { color: Colors.textMuted, fontSize: FontSize.sm, textAlign: 'center' }, card: { backgroundColor: Colors.bgCard, borderRadius: Radius.lg, padding: Spacing.lg, marginBottom: Spacing.sm }, cardTop: { flexDirection: 'row', alignItems: 'center', gap: Spacing.md }, deliveryIcon: { width: 40, height: 40, borderRadius: 20, backgroundColor: Colors.accentLight, alignItems: 'center', justifyContent: 'center' }, cardMain: { flex: 1 }, cardTitle: { color: Colors.primary, fontFamily: 'PlusJakartaSans_600SemiBold', fontSize: FontSize.md }, cardSubtitle: { color: Colors.textSecondary, fontSize: FontSize.sm, marginTop: 2 }, badge: { backgroundColor: '#FFF4D6', borderRadius: Radius.full, paddingHorizontal: Spacing.sm, paddingVertical: 5 }, doneBadge: { backgroundColor: Colors.accentLight }, badgeText: { color: Colors.primary, fontSize: FontSize.xs, fontFamily: 'PlusJakartaSans_600SemiBold' }, deadline: { color: Colors.primary, fontFamily: 'PlusJakartaSans_600SemiBold', marginTop: Spacing.md }, meta: { color: Colors.textSecondary, fontSize: FontSize.sm, marginTop: 4 }, actions: { flexDirection: 'row', justifyContent: 'flex-end', gap: Spacing.md, marginTop: Spacing.md }, conclude: { color: Colors.accent, fontFamily: 'PlusJakartaSans_600SemiBold' }, action: { color: Colors.primary, fontFamily: 'PlusJakartaSans_600SemiBold' }, cancel: { color: Colors.danger, fontFamily: 'PlusJakartaSans_600SemiBold' }, fab: { position: 'absolute', right: 24, bottom: 24, width: 56, height: 56, borderRadius: 28, backgroundColor: Colors.accent, alignItems: 'center', justifyContent: 'center', elevation: 6 }, overlay: { flex: 1, backgroundColor: 'rgba(0,0,0,.35)', justifyContent: 'flex-end' }, modal: { backgroundColor: Colors.bgCard, borderTopLeftRadius: Radius.xl, borderTopRightRadius: Radius.xl, padding: Spacing.xl, maxHeight: '88%' }, modalTitle: { color: Colors.primary, fontFamily: 'PlusJakartaSans_700Bold', fontSize: FontSize.lg, marginBottom: Spacing.lg }, label: { color: Colors.primary, fontFamily: 'PlusJakartaSans_600SemiBold', fontSize: FontSize.sm, marginTop: Spacing.md, marginBottom: Spacing.xs }, input: { height: 46, borderWidth: 1, borderColor: Colors.border, borderRadius: Radius.md, paddingHorizontal: Spacing.md, color: Colors.primary }, chips: { gap: Spacing.sm }, chip: { borderWidth: 1, borderColor: Colors.border, borderRadius: Radius.full, paddingHorizontal: Spacing.md, paddingVertical: Spacing.sm }, chipActive: { backgroundColor: Colors.primary, borderColor: Colors.primary }, chipText: { color: Colors.textSecondary, fontSize: FontSize.sm }, chipTextActive: { color: '#FFF' }, expenseOption: { flexDirection: 'row', alignItems: 'center', gap: Spacing.sm, marginTop: Spacing.md }, expenseText: { color: Colors.textSecondary }, modalActions: { flexDirection: 'row', alignItems: 'center', justifyContent: 'flex-end', gap: Spacing.lg, marginTop: Spacing.lg }, saveButton: { backgroundColor: Colors.accent, borderRadius: Radius.full, paddingHorizontal: Spacing.lg, paddingVertical: Spacing.md }, saveText: { color: '#FFF', fontFamily: 'PlusJakartaSans_600SemiBold' } });
