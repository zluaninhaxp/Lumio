import { useMemo, useState } from 'react';
import { View, Text, StyleSheet, TouchableOpacity, Modal, TextInput, Alert, ScrollView } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Ionicons } from '@expo/vector-icons';
import { useRouter } from 'expo-router';
import { Colors, Spacing, Radius, FontSize } from '../../src/constants/theme';
import { FornecedorItem, useAppStore } from '../../src/store';
import { suggestedDueDate } from '../../src/utils/supplier';

const EMPTY_FORM = { name: '', contact: '', paymentTerm: '', notes: '' };
const money = (value: number) => `R$ ${Math.abs(value).toFixed(2).replace('.', ',')}`;

export default function FornecedoresScreen() {
  const router = useRouter();
  const { fornecedorItems, transactions, addFornecedorItem, updateFornecedorItem, removeFornecedorItem, linkTransactionToSupplier, setPluginActivation } = useAppStore();
  const [query, setQuery] = useState('');
  const [expandedId, setExpandedId] = useState<string | null>(null);
  const [modalVisible, setModalVisible] = useState(false);
  const [editingId, setEditingId] = useState<string | null>(null);
  const [form, setForm] = useState(EMPTY_FORM);
  const [unlinkedTransactionId, setUnlinkedTransactionId] = useState<string | null>(null);

  const filtered = useMemo(() => {
    const q = query.trim().toLowerCase();
    return fornecedorItems.filter((item) => !q || `${item.name} ${item.contact}`.toLowerCase().includes(q));
  }, [fornecedorItems, query]);
  const unlinkedExpenses = transactions.filter((transaction) => transaction.amount < 0 && transaction.category === 'Fornecedores' && !transaction.supplierId);

  const openAdd = () => { setEditingId(null); setForm(EMPTY_FORM); setModalVisible(true); };
  const openEdit = (item: FornecedorItem) => { setEditingId(item.id); setForm({ name: item.name, contact: item.contact, paymentTerm: item.paymentTerm, notes: item.notes }); setModalVisible(true); };
  const save = () => {
    if (!form.name.trim()) return;
    const payload = { name: form.name.trim(), contact: form.contact.trim(), paymentTerm: form.paymentTerm.trim(), notes: form.notes.trim() };
    if (editingId) updateFornecedorItem(editingId, payload); else addFornecedorItem(payload);
    setModalVisible(false);
  };
  const deleteSupplier = (id: string) => Alert.alert('Excluir fornecedor', 'Despesas vinculadas ficarão sem fornecedor, mas não serão excluídas.', [{ text: 'Cancelar', style: 'cancel' }, { text: 'Excluir', style: 'destructive', onPress: () => removeFornecedorItem(id) }]);
  const linkOldExpense = (supplier: FornecedorItem) => {
    if (!unlinkedTransactionId) return;
    const transaction = transactions.find((item) => item.id === unlinkedTransactionId);
    if (!transaction) return;
    linkTransactionToSupplier(transaction.id, supplier.id, { supplierDueDate: suggestedDueDate(transaction.date, supplier.paymentTerm), supplierPaid: false });
    setUnlinkedTransactionId(null);
  };

  return <SafeAreaView style={styles.safe}>
    <View style={styles.header}><TouchableOpacity onPress={() => router.back()} style={styles.iconBtn}><Ionicons name="chevron-back" size={24} color={Colors.primary} /></TouchableOpacity><Text style={styles.headerTitle}>Fornecedores</Text><TouchableOpacity onPress={() => Alert.alert('Desativar Fornecedores', 'O módulo sai da aba Apps, mas os dados continuam guardados.', [{ text: 'Cancelar', style: 'cancel' }, { text: 'Desativar', style: 'destructive', onPress: () => { setPluginActivation('fornecedores', false); router.back(); } }])} style={styles.iconBtn}><Ionicons name="ellipsis-horizontal" size={22} color={Colors.primary} /></TouchableOpacity></View>
    <View style={styles.searchBox}><Ionicons name="search-outline" size={18} color={Colors.textMuted} /><TextInput value={query} onChangeText={setQuery} placeholder="Buscar fornecedor" placeholderTextColor={Colors.textMuted} style={styles.searchInput} /></View>
    <ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}>
      {filtered.length === 0 && <View style={styles.empty}><Ionicons name="briefcase-outline" size={46} color={Colors.textMuted} /><Text style={styles.emptyText}>{query ? 'Nenhum fornecedor encontrado.' : 'Nenhum fornecedor cadastrado ainda.'}</Text></View>}
      {filtered.map((supplier) => {
        const history = transactions.filter((transaction) => transaction.supplierId === supplier.id && transaction.amount < 0);
        const debt = history.filter((transaction) => !transaction.supplierPaid).reduce((sum, transaction) => sum + Math.abs(transaction.amount), 0);
        const expanded = expandedId === supplier.id;
        return <View key={supplier.id} style={styles.card}><TouchableOpacity style={styles.cardHeader} onPress={() => setExpandedId(expanded ? null : supplier.id)}><View style={styles.avatar}><Text style={styles.avatarText}>{supplier.name.slice(0, 1).toUpperCase()}</Text></View><View style={styles.cardMain}><Text style={styles.cardTitle}>{supplier.name}</Text><Text style={styles.cardSubtitle}>{supplier.contact || 'Sem contato informado'} · {supplier.paymentTerm || 'Prazo não informado'}</Text></View><Ionicons name={expanded ? 'chevron-up' : 'chevron-down'} size={18} color={Colors.textMuted} /></TouchableOpacity><View style={styles.summaryRow}><Text style={styles.summaryLabel}>{history.length} compra(s) vinculada(s)</Text><Text style={[styles.total, debt === 0 && styles.paidTotal]}>{debt > 0 ? `Deve ${money(debt)}` : 'Sem saldo devedor'}</Text></View>{expanded && <View style={styles.details}>{!!supplier.notes && <Text style={styles.notes}>Observações: {supplier.notes}</Text>}<Text style={styles.historyTitle}>Histórico de compras</Text>{history.length === 0 ? <Text style={styles.muted}>Nenhuma despesa vinculada.</Text> : history.map((transaction) => <View key={transaction.id} style={styles.historyRow}><Text style={styles.muted}>{transaction.date} · {transaction.description}</Text><Text style={transaction.supplierPaid ? styles.paidText : styles.debtText}>{transaction.supplierPaid ? 'Paga' : money(transaction.amount)}</Text></View>)}<View style={styles.cardActions}><TouchableOpacity onPress={() => openEdit(supplier)}><Text style={styles.actionText}>Editar cadastro</Text></TouchableOpacity><TouchableOpacity onPress={() => deleteSupplier(supplier.id)}><Text style={styles.deleteText}>Excluir</Text></TouchableOpacity></View></View>}</View>;
      })}
      {unlinkedExpenses.length > 0 && <View style={styles.migration}><Text style={styles.migrationTitle}>Despesas sem fornecedor</Text><Text style={styles.muted}>Associe as despesas antigas da categoria Fornecedores.</Text>{unlinkedExpenses.map((transaction) => <TouchableOpacity key={transaction.id} style={styles.migrationRow} onPress={() => setUnlinkedTransactionId(transaction.id)}><View style={styles.cardMain}><Text style={styles.cardTitle}>{transaction.description}</Text><Text style={styles.muted}>{transaction.date}</Text></View><Text style={styles.debtText}>{money(transaction.amount)}</Text><Ionicons name="link-outline" size={18} color={Colors.accent} /></TouchableOpacity>)}</View>}
    </ScrollView>
    <TouchableOpacity style={styles.fab} onPress={openAdd}><Ionicons name="add" size={28} color="#FFFFFF" /></TouchableOpacity>
    <Modal visible={modalVisible} transparent animationType="slide" onRequestClose={() => setModalVisible(false)}><View style={styles.modalOverlay}><View style={styles.modalCard}><Text style={styles.modalTitle}>{editingId ? 'Editar fornecedor' : 'Novo fornecedor'}</Text><TextInput style={styles.input} placeholder="Nome" placeholderTextColor={Colors.textMuted} value={form.name} onChangeText={(v) => setForm((f) => ({ ...f, name: v }))} autoFocus /><TextInput style={styles.input} placeholder="Contato (opcional)" placeholderTextColor={Colors.textMuted} value={form.contact} onChangeText={(v) => setForm((f) => ({ ...f, contact: v }))} /><TextInput style={styles.input} placeholder="Prazo padrão (ex.: 30 dias)" placeholderTextColor={Colors.textMuted} value={form.paymentTerm} onChangeText={(v) => setForm((f) => ({ ...f, paymentTerm: v }))} /><TextInput style={[styles.input, styles.notesInput]} placeholder="Observações" placeholderTextColor={Colors.textMuted} value={form.notes} onChangeText={(v) => setForm((f) => ({ ...f, notes: v }))} multiline /><View style={styles.modalActions}><TouchableOpacity style={styles.modalCancel} onPress={() => setModalVisible(false)}><Text style={styles.modalCancelText}>Cancelar</Text></TouchableOpacity><TouchableOpacity style={styles.modalConfirm} onPress={save}><Text style={styles.modalConfirmText}>Salvar</Text></TouchableOpacity></View></View></View></Modal>
    <Modal visible={unlinkedTransactionId !== null} transparent animationType="fade" onRequestClose={() => setUnlinkedTransactionId(null)}><View style={styles.modalOverlay}><View style={styles.linkCard}><Text style={styles.modalTitle}>Associar fornecedor</Text><Text style={styles.muted}>Escolha o cadastro para esta despesa.</Text>{fornecedorItems.map((supplier) => <TouchableOpacity key={supplier.id} style={styles.linkOption} onPress={() => linkOldExpense(supplier)}><Text style={styles.cardTitle}>{supplier.name}</Text><Text style={styles.muted}>{supplier.paymentTerm || 'Prazo não informado'}</Text></TouchableOpacity>)}<TouchableOpacity style={styles.modalCancel} onPress={() => setUnlinkedTransactionId(null)}><Text style={styles.modalCancelText}>Cancelar</Text></TouchableOpacity></View></View></Modal>
  </SafeAreaView>;
}

const styles = StyleSheet.create({
  safe: { flex: 1, backgroundColor: Colors.bg }, header: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: Spacing.lg, paddingVertical: Spacing.md }, iconBtn: { padding: Spacing.xs }, headerTitle: { fontFamily: 'PlusJakartaSans_700Bold', fontSize: FontSize.lg, color: Colors.primary }, searchBox: { flexDirection: 'row', alignItems: 'center', gap: Spacing.sm, marginHorizontal: Spacing.xl, marginBottom: Spacing.md, paddingHorizontal: Spacing.lg, backgroundColor: Colors.bgCard, borderRadius: Radius.full, borderWidth: 1, borderColor: Colors.border }, searchInput: { flex: 1, height: 44, color: Colors.primary }, content: { paddingHorizontal: Spacing.xl, paddingBottom: 100 }, empty: { alignItems: 'center', paddingTop: 80, gap: Spacing.md }, emptyText: { fontFamily: 'PlusJakartaSans_600SemiBold', color: Colors.textSecondary }, card: { backgroundColor: Colors.bgCard, borderRadius: Radius.lg, padding: Spacing.lg, marginBottom: Spacing.sm }, cardHeader: { flexDirection: 'row', alignItems: 'center', gap: Spacing.md }, avatar: { width: 38, height: 38, borderRadius: 19, backgroundColor: '#FFF1E6', alignItems: 'center', justifyContent: 'center' }, avatarText: { color: '#D97706', fontFamily: 'PlusJakartaSans_700Bold' }, cardMain: { flex: 1 }, cardTitle: { fontFamily: 'PlusJakartaSans_600SemiBold', color: Colors.primary, fontSize: FontSize.md }, cardSubtitle: { color: Colors.textSecondary, fontSize: FontSize.sm, marginTop: 2 }, summaryRow: { flexDirection: 'row', justifyContent: 'space-between', marginTop: Spacing.md }, summaryLabel: { color: Colors.textMuted, fontSize: FontSize.xs }, total: { color: Colors.danger, fontFamily: 'PlusJakartaSans_700Bold', fontSize: FontSize.sm }, paidTotal: { color: Colors.accent }, details: { borderTopWidth: 1, borderTopColor: Colors.border, marginTop: Spacing.md, paddingTop: Spacing.md }, notes: { color: Colors.textSecondary, marginBottom: Spacing.md }, historyTitle: { fontFamily: 'PlusJakartaSans_600SemiBold', color: Colors.primary, marginBottom: Spacing.sm }, historyRow: { flexDirection: 'row', justifyContent: 'space-between', paddingVertical: 5, gap: Spacing.sm }, muted: { color: Colors.textMuted, fontSize: FontSize.sm, flex: 1 }, debtText: { color: Colors.danger, fontFamily: 'PlusJakartaSans_600SemiBold' }, paidText: { color: Colors.accent, fontFamily: 'PlusJakartaSans_600SemiBold' }, cardActions: { flexDirection: 'row', justifyContent: 'space-between', marginTop: Spacing.md }, actionText: { color: Colors.accent, fontFamily: 'PlusJakartaSans_600SemiBold' }, deleteText: { color: Colors.danger, fontFamily: 'PlusJakartaSans_600SemiBold' }, migration: { backgroundColor: Colors.accentLight, borderRadius: Radius.lg, padding: Spacing.lg, marginTop: Spacing.md }, migrationTitle: { fontFamily: 'PlusJakartaSans_700Bold', color: Colors.primary, marginBottom: Spacing.xs }, migrationRow: { flexDirection: 'row', alignItems: 'center', gap: Spacing.sm, paddingVertical: Spacing.md, borderBottomWidth: 1, borderBottomColor: '#CDEDE4' }, fab: { position: 'absolute', bottom: 24, right: 24, width: 56, height: 56, borderRadius: 28, backgroundColor: Colors.accent, alignItems: 'center', justifyContent: 'center', elevation: 6 }, modalOverlay: { flex: 1, backgroundColor: 'rgba(0,0,0,0.4)', justifyContent: 'flex-end' }, modalCard: { backgroundColor: Colors.bgCard, borderTopLeftRadius: Radius.xl, borderTopRightRadius: Radius.xl, padding: Spacing.xxl, paddingBottom: 40, gap: Spacing.md }, linkCard: { backgroundColor: Colors.bgCard, margin: Spacing.xl, borderRadius: Radius.xl, padding: Spacing.xxl, gap: Spacing.md }, modalTitle: { fontFamily: 'PlusJakartaSans_700Bold', fontSize: FontSize.xl, color: Colors.primary }, input: { backgroundColor: Colors.bg, borderRadius: Radius.md, padding: Spacing.lg, color: Colors.primary }, notesInput: { minHeight: 72, textAlignVertical: 'top' }, modalActions: { flexDirection: 'row', gap: Spacing.md, marginTop: Spacing.sm }, modalCancel: { flex: 1, padding: Spacing.lg, borderRadius: Radius.md, borderWidth: 1, borderColor: Colors.border, alignItems: 'center' }, modalCancelText: { fontFamily: 'PlusJakartaSans_600SemiBold', color: Colors.textSecondary }, modalConfirm: { flex: 1, padding: Spacing.lg, borderRadius: Radius.md, backgroundColor: Colors.accent, alignItems: 'center' }, modalConfirmText: { fontFamily: 'PlusJakartaSans_600SemiBold', color: '#FFFFFF' }, linkOption: { padding: Spacing.md, borderRadius: Radius.md, backgroundColor: Colors.bg },
});
